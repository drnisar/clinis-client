import React from "react";
import Calendar from "./Calendar";
import ApptsForm from "./ApptsForm";

const ParentComponent = () => {
  return (
    <div>
      <Calendar />
      <ApptsForm />
    </div>
  );
};

export default ParentComponent;
