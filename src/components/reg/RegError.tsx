const RegError = () => {
  return (
    <div>
      <p>
        Select a Record on the left panel to display the details or search in
        the search bar
      </p>
    </div>
  );
};

export default RegError;
